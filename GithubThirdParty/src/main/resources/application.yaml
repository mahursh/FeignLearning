spring:
  application:
    name: GithubThirdParty



  profiles:
    active: docker



  datasource:
    driver-class-name: oracle.jdbc.OracleDriver
    url: jdbc:oracle:thin:@172.16.60.65:1521:ORCL
    username: KAR_AMOOZ
    password: kar_amooz

  jpa:
    hibernate:
      ddl-auto: none

  flyway:
    baseline-on-migrate: true
    locations: classpath:db/migration
#    out-of-order: true

server:
  port: 80



logging:
  level:
    root: info
    org.springframework.web: info
    org.zalando.logbook: trace


logbook:
  filter.enabled: true
  secure-filter.enabled: true
  format.style: json
  strategy: default
  obfuscate:
    headers:
      - Authorization
      - X-Secret
    parameters:
      - access_token
      - password

#+++++++++++++++++++++++++++++++++++++++++

#logging:
#  level:
#    root: info
#    org:
#      springframework:
#        web: debug
#
#  file:
#    name: logs/application.log

#  pattern:
#    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"
#    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"


#+++++++++++++++++++++++++++++++++++++++++
#
#logging:
#  level:
#    org:
#      zalando:
#        logbook:
#          Logbook: trace







